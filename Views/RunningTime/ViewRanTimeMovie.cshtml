@model IEnumerable<CinemaApp.ViewModels.RunningTimeViewModel>
@{
    ViewData["Title"] = "Program filme";
}

<h3>@ViewData["Title"] <small> (@ViewBag.MovieProgram)</small></h3>

<div>

    <table class="table">
        <thead>
            <tr>
                <th class=" d-none d-table-cell p-3">
                    Nume Film
                </th>
                <th class="d-none d-md-table-cell p-3">
                    Sala
                </th>
                <th class="d-none d-md-table-cell p-3">
                    Ora de inceput
                </th>
                <th class="d-none d-md-table-cell p-3">
                    Ora de sfarsit
                </th>
                <th width="116"></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var movie in Model)
            {
                <tr>
                    <td class="d-table-cell align-middle">
                        <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.MovieID" class="custom-link">
                            @Html.DisplayFor(x => movie.MovieName)
                        </a>
                    </td>

                    <td class=" d-none d-md-table-cell align-middle">
                        @Html.DisplayFor(x => movie.CinemaName)
                    </td>

                    <td class=" d-none d-md-table-cell align-middle">
                        @Html.DisplayFor(x => movie.StartDate)
                    </td>

                    <td class=" d-none d-md-table-cell align-middle">
                        @Html.DisplayFor(x => movie.EndDate)
                    </td>

                    <td>
                        <div class="d-flex">
                            <a class="btn btn-primary ml-2" asp-controller="Person" asp-action="Edit" asp-route-id="@movie.MovieID"
                               data-toggle="tooltip" data-html="true" title="Editare">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a class="btn btn-primary ml-2" asp-controller="Person" asp-action="Delete" asp-route-id="@movie.MovieID"
                               data-toggle="tooltip" data-html="true" title="Stergere">
                                <i class="fas fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

<style>
    a:hover {
        color: black;
    }

    body {
        background-color: white;
        padding-bottom: 20px;
    }

    .custom-link {
        color: black;
        font-weight: 500;
    }

    .h3 {
        margin: 20px 0 !important;
        font-weight: 500 !important;
    }
</style>





@*<header>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    </header>



    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark btnCreateRunTime">Adauga interval orar</a>
        </li>

    </ul>


    <div class="navbar-nav">
        <div class="modal fade task-modal-content show" id="CreateRunTime" tabindex="-1" role="dialog" aria-labelledby="CreateRunTimeTitle" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="CreateRunTimeTitle">Adauga un nou interval orar</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <form class="p-2">
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label>Aloca pe</label>
                                        <select class="form-control form-control-light"  id="task-employee">
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="example-number-input">Numar de ore necesare</label>
                                        <input class="form-control form-control-light" type="number" id="numberOfHours-edit">
                                    </div>
                                    <div class="text-right">
                                        <button type="button" class="btn btn-light" data-dismiss="modal">Inapoi</button>
                                        <button type="button" class="btn btn-primary" id="save-button" data-dismiss="modal">Salveaza modificari</button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>

                </div>
            </div>
        </div>
    </div>


    <script>
        $('a.btnCreateRunTime').on('click', function (e) {
            e.preventDefault();
            $('#CreateRunTime').modal('show');
            $.ajax({
                type: 'GET',
                url: `/RunningTime/AddMovieRunTime`,
                async: false,
                cache: false,
                timeout: 5000,
                error: function () {
                    return true;
                },
                success: function (data) {
                    response = data.data;
                }
            });

        })
    </script>*@